# Sprint 12
정렬 알고리즘 기초 연습 프로젝트

## 스프린트 개인 목표
그냥 재미삼아 추가하는 개인 목표입니다.

- [x] 정렬 알고리즘 구현하기
   - 선택 정렬  
      숫자형 배열을 파라미터로 받고, 해당 배열을 수정합니다.
   - 삽입 정렬  
      숫자형 배열을 파라미터로 받고, 해당 배열을 수정합니다.
   - 병합 정렬  
      숫자형 배열을 파라미터로 받고, 정렬된 새로운 배열을 반환합니다.
   - 빠른 정렬  
      숫자형 배열을 파라미터로 받고, 해당 배열을 수정합니다.

- [x] 반복문은 `while` 만, 조건문은 `if` 만 써보기
   - 특별한 이유는 없고, 그냥 제한 조건을 줘보고 싶었습니다. 다하고 나서 시간이 남아서..

- [x] 정렬 알고리즘 대신 해주는 API 구현하기  
   > http://localhost:4000/api/sorts  

   | method | endpoint | params |
   | :--- | :--- | :--- |
   | GET | / | - |
   | POST | /selection | number\[\] | 
   | POST | /insertion | number\[\] | 
   | POST | /merge | number\[\] | 
   | POST | /quick | number\[\] | 

- [x] `jest`, `supertest` 테스트 코드 추가하기
   - 테스트 코드 작성하는 걸 익숙해지려고 연습 삼아 추가했습니다.

- [x] 간단한 테스트용 프론트 추가